
// clang-format off

// Why adaptor accepts Container as template parameter but not as template template?
// --- Container is able to have several template parameters and
// even values (std::array, so variadic is also not suitable)

// clang-format on
